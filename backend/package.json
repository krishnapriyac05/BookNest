{
  "name": "booknest-backend",
  "version": "1.0.0",
  "private": true,
  "description": "JSON Server backend for the BookNest online book and stationery store",
  "scripts": {
    "start": "json-server data/db.json",
    "dev": "json-server --watch data/db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}